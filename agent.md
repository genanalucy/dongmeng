# 项目 Agent 工作规范

## 1. 项目定位

- 本项目开发一个实时翻译 App，最终主要面向 Android，后续可能支持 iOS。
- 当前第一期是 Web MVP，固定在 macOS + Chrome 上开发和测试。
- 第一阶段的产品、架构、协议、功能边界、开发顺序和验收标准，以根目录的 [`web-macos-translation-app-spec.md`](./web-macos-translation-app-spec.md) 为准。
- 第一阶段不得擅自扩大到 Android、iOS、云端 Backend、用户系统、数据库、支付或规格明确排除的其他功能。
- 当前首要目标是验证：Mac 麦克风采集、火山引擎同声传译、浏览器实时字幕与 TTS，以及一副 TWS 耳机左右声道独立播放。

## 2. 直接调度责任

- 主 Agent 是本项目的直接调度人，对任务拆解、人员/模型选择、进度、代码质量、验证结果和最终交付负责。
- 主 Agent 不得把最终判断完全交给子 Agent；子 Agent 的结论和代码必须由主 Agent 检查、整合和验证。
- 开始任务前，先阅读相关现有实现、规格、项目约定和依赖文档，不得凭猜测堆砌代码。
- 对复杂或多步骤任务建立清晰的任务列表，并确保同一时间只有一个主任务处于进行中。

## 3. 子 Agent 与 herdr 调度规则

- 积极使用子 Agent，尤其是代码实现、测试、技术调查、官方文档查阅、独立审查等可拆分工作。
- 调用子 Agent 时，优先通过 `herdr` 创建独立 workspace、tab 或 pane，将子 Agent 的执行过程展示出来；使用明确的 pane 别名，如 `coder`、`tests`、`researcher`、`reviewer`。
- 可并行且互不依赖的任务应并行调度，不得无意义串行等待；存在依赖时必须按依赖顺序执行。
- 不要为了形式而调用子 Agent。单文件、低风险、答案明确的简单任务可由主 Agent直接完成。
- 不得让多个 Agent 同时修改同一文件或同一区域；如需并行写代码，应划分互不重叠的文件范围或使用隔离 worktree。
- 子 Agent 完成后，主 Agent 必须读取实际变更、检查 diff 并运行验证，不能只依据子 Agent 的总结宣布完成。

### 强制模型调度矩阵

模型首先按任务复杂度选择，工作类型只作为复杂度判断的参考。代码、测试、调查和文档任务都适用同一套分级，不得机械地按任务名称固定模型。

| 难度 | 必选模型与思考强度 | 判断标准 | 典型任务 |
| --- | --- | --- | --- |
| 简单 | `aether/gpt-5.6-luna` | 目标明确、改动局部、风险低、基本不涉及跨模块推理 | 官方文档和 API 查阅、事实核验、简单调查、机械性文档更新、局部样式或文案修改、明确的小测试、单文件低风险代码修改 |
| 中等 | `aether/gpt-5.6-terra`，`high` | 需要理解现有实现并进行工程判断，涉及常规功能、Bug 或少量模块协作 | 常规功能开发、一般 Bug 定位与修复、单元/集成测试、跨少量文件修改、普通重构、常规代码审查 |
| 高复杂度 | `aether/gpt-5.6-sol`，`high` | 涉及核心架构、高风险协议、实时并发、跨模块复杂状态，或失败成本高 | 架构设计、火山 AST 2.0 WebSocket / Protobuf、AudioWorklet 重采样、实时 PCM、播放队列、声道隔离、并发状态机、跨 React/Web Audio/Go 的疑难故障和核心方案审查 |

> 本机模型目录中的精确 provider/model ID 是 `aether/gpt-5.6-luna`、`aether/gpt-5.6-terra` 和 `aether/gpt-5.6-sol`。通过 CLI 或 `herdr` 启动时必须显式指定 `--provider aether`，不得只传裸 model ID，以免误匹配其他 provider。用户原始规则中的 `gpt-.6-terra high` 按 `aether/gpt-5.6-terra`、`high` 执行。

### 难度判断和调用要求

- 简单任务必须优先使用 `aether/gpt-5.6-luna`；不能因为任务属于“代码”或“测试”就自动使用 Terra。
- 中等任务默认使用 `aether/gpt-5.6-terra high`；包括大多数需要工程判断的代码实现、Bug 修复和测试工作。
- 高复杂度任务必须使用 `aether/gpt-5.6-sol high`；不得为了节省资源将中等或高复杂度任务降级给更低模型。
- 调查和文档通常属于简单任务，使用 Luna；若需要复杂方案比较、跨版本验证或深入代码推理，应按实际难度升级到 Terra high 或 Sol high。
- 主 Agent 在调用子 Agent 前必须明确写出：任务难度、选择的模型、思考强度、Agent 职责、预期产物以及允许修改的文件边界。
- 如果任务执行中发现复杂度高于预判，应停止当前实现并升级模型；不得让能力不足的模型继续猜测式修改。
- 子 Agent 首次修改或验证失败后，不得通过连续换模型递归试错；必须遵守本文件的 Bug 修改止损规则，先向用户汇报问题和建议，再决定是否升级模型继续。
- 模型升级不替代主 Agent 审核。无论使用哪个模型，主 Agent 都必须检查实际代码、diff、测试结果和风险。

## 4. API Key、测试数据与隐私阶段策略

- 当前项目处于本地技术验证阶段，暂不开发额外的隐私设置、隐私 UI、内容脱敏功能或数据合规流程，除非用户另行要求。
- 用户提供 API Key 后，可直接用于当前本地开发和联调，不需要因产品隐私功能尚未完成而阻塞测试。
- 翻译内容按原文处理，不做规格之外的额外脱敏、改写、摘要或过滤，确保真实验证翻译链路。
- API Key 只用于必要的本地服务配置，不得写入前端代码、提交到 Git、输出到日志、测试快照或对用户无关的第三方渠道。
- 按规格优先通过本地环境变量 `VOLCENGINE_API_KEY` 传给 Go Local Agent；不得放入 `VITE_*`、Browser、React source 或 `localStorage`。
- 不持久化完整 PCM 音频或用户语音文件，除非用户明确要求某个本地调试用例；调试产物不得提交到仓库。
- “暂不做隐私设置”仅表示不开发额外产品功能，不表示可以泄露凭据、提交密钥或绕过运行环境的权限与安全机制。

## 5. Bug 修改止损规则

- 修复 Bug 前必须先复现、定位根因，并说明预期修改点和验证方法。
- 只做有依据的最小修改，不得在未理解根因时连续试错或扩大重构范围。
- 如果一次有依据的修复未解决问题，或修改/验证失败：
  1. 立即停止递归式继续修改；
  2. 保留并收集错误信息、复现步骤、相关日志和已验证的假设；
  3. 检查本次变更是否应安全回退，但不得破坏用户已有工作；
  4. 向用户汇报问题所在、尝试过什么、为什么失败、当前影响范围及建议的下一步；
  5. 等待用户决定，或在获得明确授权后再进行下一轮修改。
- 不得用更多补丁掩盖前一个失败补丁，不得宣称未通过验证的 Bug 已修复。

## 6. 开发与验证要求

- 严格遵循规格中的职责边界：Browser 负责 UI、麦克风、重采样、PCM、PTT、左右声道和播放；Go Local Agent 负责 API Key、火山 WebSocket、Protobuf 和协议转换。
- 使用官方火山 AST 2.0 Protobuf 或官方 Go Demo，不得自行猜测协议。
- TypeScript 禁止使用 `any`；必须处理空值、异常路径、设备断开、并发状态和资源释放。
- 新功能应包含与风险匹配的单元测试、集成测试或可复现的 Mac 手工验收步骤。
- 每次代码变更后，运行项目中可用且相关的测试、类型检查、lint 和构建；核心音频链路还需按规格进行 macOS + Chrome + TWS 实机验证。
- 验证未执行、覆盖不足或命令失败时必须明确说明，不得宣布任务完成。

## 7. GitHub 存档与版本更新

- 项目 GitHub 仓库：<https://github.com/genanalucy/dongmeng.git>。
- 每完成一个新功能，都必须及时存档更新，不积压多个功能后再一次性提交。
- 标准流程：
  1. 检查工作区和变更范围；
  2. 排除无关文件、密钥、用户本地配置、日志和构建产物；
  3. 完成相关验证；
  4. 更新必要的 README、规格说明或测试记录；
  5. 创建清晰、单一职责的 Git commit；
  6. 将更新推送到上述 GitHub 仓库的正确分支。
- 优先遵循仓库已有提交规范；没有现成规范时使用英文 Conventional Commits，例如 `feat(web): add stereo ear test`。
- 推送前确认 remote 指向正确仓库，不得 force push、重写公共历史或覆盖他人提交，除非用户明确授权。
- 如果目录尚未初始化/克隆为 Git 仓库、没有实际变更、验证失败、权限不足、远端认证失败或存在冲突，则不得假装已存档；应明确汇报阻塞原因和所需操作。
- 工作区存在与当前任务无关的修改时，不修改、不暂存、不提交这些内容，并在汇报中说明。

## 8. 每次任务的执行流程

1. 阅读本文件、`AGENTS.md`、相关规格和现有实现。
2. 明确目标、影响范围、验收标准和潜在风险。
3. 判断是否拆分子任务，以及使用 `gpt-5.6-terra`、`gpt-5.6-luna` 或 `gpt-5.6-sol high`。
4. 需要子 Agent 时，通过 `herdr` 可视化调度，并划分清楚职责与文件边界。
5. 实现最小且可维护的变更。
6. 主 Agent 审查实际 diff，执行测试、类型检查、lint、构建及必要的 Mac 实机验证。
7. 新功能验证通过后立即提交并更新 GitHub；无法提交或推送时明确汇报。
8. 最终汇报修改内容、验证结果、commit hash、推送状态以及仍存风险。

## 9. 规则优先级

- 必须遵守运行环境的系统规则、权限插件、安全限制和项目上层规范。
- 本文件与 `web-macos-translation-app-spec.md` 共同约束项目执行：规格定义产品与技术实现，本文件定义 Agent 的调度和工作流程。
- 若二者冲突，涉及产品和技术验收时以规格为准；涉及调度、Bug 止损和版本存档时以本文件为准。
- 对冲突、歧义或高风险选择不得自行猜测，应先向用户说明并请求确认。
