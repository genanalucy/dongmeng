# Android Internal Beta 真机验收记录（token-safe 模板）

> 本文件是 Task 8 的受控记录模板。填写前确认仅在 `/tmp/dngmeng-internal-beta-integration` 的审查范围内操作。不得填写或粘贴密码、邮箱、access/refresh/session JWT、token、DSN、API key、Provider key、`.env` 内容、数据库原始行、完整 URL query、logcat、原始音频、录音或对话/字幕正文。

## A. 记录元数据

| 字段 | 填写值 |
| --- | --- |
| 记录状态（仅选一项：通过 / 失败 / 未运行 / 阻塞） | `[ ]` |
| 验收日期（UTC） | `YYYY-MM-DD` |
| 操作员代号（不得填写真实邮箱/姓名） | `operator-____` |
| 验收用户代号（不得填写账号标识） | `tester-____` |
| 设备代号 / Android 大版本 | `device-____ / Android __` |
| App version / variant | `1.0-debug / com.verba.interpretation.debug` |
| APK SHA-256（可选；仅填构建产物摘要，不上传 APK） | `sha256: ____` |
| 网络类别（不填 SSID、IP、代理凭据） | `可信 internal beta 网络 / 其他：____` |
| 清单版本 | `internal-beta-android-device-checklist.md @ ____` |

## B. 启动前提记录

| 检查项 | 结果（通过/失败/未运行） | 安全备注（不写秘密） |
| --- | --- | --- |
| Cloud API readiness | `____` | 仅记录 HTTP 状态/通用结果：`____` |
| Agent health | `____` | 仅记录 HTTP 状态/通用结果：`____` |
| Debug Cloud API 地址 | `通过/失败` | 只写“为批准的 internal beta HTTP 地址”或“仍是 loopback”；不要重复完整敏感 URL |
| Agent WS 地址 | `通过/失败` | 只写“为批准的 internal beta WS 地址”；不要记录 query/header/token |
| Agent session JWT 校验保持启用 | `通过/失败/未运行` | 不记录配置值 |
| Cloud API / Agent 配置一致性 | `通过/失败/未运行` | 不记录 issuer/audience/key |
| 真实 Provider / 服务是否被本记录流程启动 | `否` | 本模板默认不启动、不探测；如实际执行必须另行获批并记录为阻塞/未运行 |
| 数据库连接/写入是否由本记录流程执行 | `否` | 本模板不接 DB；usage 只能由授权操作员独立只读确认 |

## C. 用户操作结果

| 步骤 | 结果 | 只记录安全事实 |
| --- | --- | --- |
| 安装 Debug APK | `通过/失败` | 安装错误通用文本：`____` |
| 麦克风权限 | `通过/拒绝/未运行` | 系统权限结果：`____` |
| 注册或登录普通用户 | `通过/失败/未运行` | 不填写账号、密码、token；页面通用错误：`____` |
| trial / entitlement 显示 | `通过/无权益/失败/未运行` | 只写状态，不写 entitlement ID/日期关联个人数据：`____` |
| 进入单人同传 | `通过/失败/未运行` | `____` |
| 源/目标语言 | `中文 → English` | 固定验收组合 |
| 验证句 | `已说 / 未说` | 不抄录任何转写或字幕内容 |
| 英文字幕 | `出现/未出现/未运行` | 只填结果，不写字幕正文 |
| 英文语音 | `出现/未出现/未运行` | 只填结果，不发送录音 |
| 点击结束/停止 | `通过/失败/未运行` | `____` |
| UI 最终状态 | `空闲/错误/卡住/崩溃/未运行` | 可见通用错误文本（若无秘密）：`____` |
| 截图 | `无 / 有 1 张已脱敏` | 不在此文件嵌入截图；存放位置/权限：`____` |

## D. 收尾与业务证据（最小化）

仅由授权操作员按受控、只读程序填写；不粘贴 API 响应、数据库行或日志。

| 检查项 | 结果 | 脱敏证据摘要 |
| --- | --- | --- |
| 麦克风已停止 | `通过/失败/未运行` | `____` |
| Cloud session 已结束/撤销 | `通过/失败/未运行` | 只写通用状态，不写 session ID |
| 同一 session usage 行数 | `1 / 0 / >1 / 未运行` | 只填计数；若非 1，记录缺陷编号：`____` |
| usage 仅为聚合字段 | `通过/失败/未运行` | 只写“字段范围符合约定/不符合”，不写字段值 |
| PCM/原文/译文/JWT/密码未持久化 | `通过/失败/未运行` | 只写抽查结论，不附原始数据 |
| usage 上报失败是否阻断 UI 收口 | `否 / 是 / 未运行` | `____` |
| 重复会话/活跃会话残留 | `无 / 有 / 未运行` | `____` |

## E. 失败收口

- [ ] 已停止录音并结束/关闭 App，未继续重复创建会话。
- [ ] 仅保留了步骤编号、时间、版本、设备代号、通用可见错误和已脱敏截图。
- [ ] 未收集或发送完整 logcat、bugreport、网络包、HTTP header/body、数据库输出、环境文件、录音或会话正文。
- [ ] 如疑似 token/DSN/API key/密码/个人数据泄露，已停止验收并转受控安全事件流程；本记录不附材料。
- [ ] 已创建内部缺陷/事件编号（不含秘密）：`INC/BUG-____`。

## F. 结论

**结论（只选一项）：** `通过 / 失败 / 未运行 / 阻塞`

安全摘要（不含秘密、账号、token 或对话内容）：

> `____`

阻塞/限制（如适用）：

> `____`

复核人代号：`reviewer-____`  日期（UTC）：`____`

## G. 预定义“不宣称”边界

- 没有用户真机或批准的受控设备结果，不得宣称真实中译英通过。
- 自动化测试、Task 5 会话生命周期证据、Task 7 fake-Provider 授权证据和 Debug APK 构建成功，不等于真实 Provider 翻译成功。
- 本记录通过也不等于生产就绪；HTTPS/WSS、正式域名、Release 签名、Secret Manager、托管数据库、备份、监控/告警、限流和部署恢复仍是生产前置条件。
